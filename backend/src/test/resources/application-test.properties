# Application.properties file used for testing
# Test configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/internal_control_test
spring.datasource.username=ik_root
spring.datasource.password=ik_pwd
spring.jpa.hibernate.ddl-auto=create-drop
spring.flyway.enabled=false

# JWT Configuration
jwt.secret.key=test-secret-key-that-is-long-enough-for-hs256-algorithm-at-least-256-bits
jwt.access.token.expiration=900000
jwt.refresh.token.expiration=604800000

# CORS
app.cors.allowed-origins=http://localhost:5173
